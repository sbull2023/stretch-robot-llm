# Deployment (robot + language side). ROS Noetic packages (rospy,
# actionlib, cv_bridge, tf, message types) install through apt/rosdep,
# not pip.
requests>=2.28
numpy>=1.21
pyyaml>=6.0
opencv-python>=4.5
playsound==1.2.2
